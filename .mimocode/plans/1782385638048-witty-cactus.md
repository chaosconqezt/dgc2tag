# Plan: Актуализация MD-документации проекта dgc2tag

## Цель
Привести README.md, PROJECT.md и TODO.md в соответствие с текущим состоянием кода.

## Найденные расхождения

### README.md
- В целом актуален, но есть мелкие неточности:
  - Секция "Adding a New Source" — правильная, но можно дополнить про `query` параметр в search()
  - Нет упоминания MusicBrainz как уже реализованного источника вFeatures
  - Нет упоминания `/api/library/children` (lazy load) в API
  - Нет упоминания ряда эндпоинтов: `/api/config/write-track-names`, `/api/config/write-track-artists`, `/api/webfetch`, `/api/parse-genres`, `/api/browser/status`

### PROJECT.md
- **Структура `client/src/`** — отсутствуют компоненты:
  - `ErrorBoundary.tsx` — есть в коде, нет в описании
  - `DgcResults.tsx`, `DeezerResults.tsx`, `MusicBrainzResults.tsx` — отдельные компоненты результатов, не упомянуты
  - `utils/index.ts` — barrel export, не упомянут
  - `build.ts` — не упомянут
- **Структура `server/src/`** — нет `musicbrainz.ts` и `deezer.ts` в дереве (они есть в коде!)
- **API таблица** — отсутствуют эндпоинты:
  - `POST /api/config/write-track-names`
  - `POST /api/config/write-track-artists`
  - `GET /api/library/children?dirPath=`
  - `GET /api/webfetch?url=`
  - `POST /api/parse-genres`
  - `GET /api/browser/status`
  - `GET /api/post/:id` (DGC детали)
- **SearchSource interface** — в коде `search()` принимает 3 параметра `(artist?, album?, query?)`, в PROJECT.md описано только 2
- **config.json** — `musicRoot: "m:\\soulsync"`, `outputMode: "subfolder"`, `outputFolder: "tag"` — отличается от DEFAULTS в config.ts
- **music_processed/** — в PROJECT.md описан как "результат после MOVE", но сейчас outputMode=subfolder и outputFolder="tag" → результат пишется в `music/tag/`

### TODO.md
- **Критическое**: MusicBrainz **уже реализован** как третий источник, но в секции "Новые источники данных" всё ещё описан как "Добавить MusicBrainz как третий источник поиска"
- **Реализованные фичи** — MusicBrainz не упомянут как реализованный
- **Баги**:
  - #10 `console.log` в scraper.ts (строки 218, 228, 247, 251, 259, 279) — всё ещё используют console.log вместо logger
  - #11 `abortControllerRef` в useAppContext.tsx:264 — объявлен, нигде не используется
  - #12 `fs.rmdir` в tagWriter.ts:313 — deprecated, всё ещё используется
  - #15 config.json vs config.ts DEFAULTS — расхождение сохраняется

## Файлы для изменения

1. **`README.md`** — обновить секции Features, API (полная таблица), Architecture
2. **`PROJECT.md`** — обновить структуру, API таблицу, SearchSource интерфейс, текущие источники
3. **`TODO.md`** — переместить MusicBrainz из "Новые источники" в "Реализованные фичи", обновить статус багов

## Изменения по файлам

### 1. README.md
- Добавить в Features: lazy load дерева библиотеки, MusicBrainz как третий источник
- Дополнить "Adding a New Source" — `search(artist?, album?, query?)` с тремя параметрами
- Обновить Architecture: упомянуть `musicbrainz.ts`, `deezer.ts` как отдельные файлы-реализации

### 2. PROJECT.md
- Обновить дерево `server/src/` — добавить `musicbrainz.ts`, `deezer.ts`
- Обновить дерево `client/src/` — добавить `ErrorBoundary.tsx`, `DgcResults.tsx`, `DeezerResults.tsx`, `MusicBrainzResults.tsx`, `utils/index.ts`, `build.ts`
- Добавить недостающие эндпоинты в API таблицу
- Обновить описание SearchSource — `search(artist?, album?, query?)`
- Уточнить конфигурацию: musicRoot, outputFolder, outputMode в текущем config.json
- Обновить секцию "Текущие источники" — все три источника уже реализованы

### 3. TODO.md
- Переместить MusicBrainz из "Новые источники данных" → "Реализованные фичи"
- Обновить статус багов: #10 (console.log), #11 (abortControllerRef), #12 (fs.rmdir) — подтвердить что они актуальны
- Обновить #15 (config расхождение) — указать текущие реальные значения

## Верификация
- Сравнить обновлённые описания с фактическим кодом
- Убедиться что все эндпоинты из index.ts описаны в PROJECT.md
- Убедиться что все компоненты из client/src/components/ описаны в PROJECT.md
