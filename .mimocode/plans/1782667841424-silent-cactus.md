# ProgressOverlay: diff-вывод + удаление инлайнов

## Что делаем

1. Добавить CSS-классы для overlay/modal в `index.css`
2. Переписать `ProgressOverlay.tsx` — использовать классы, показывать diff переименований
3. Передать реальные данные о переименованиях из `useTagActions.ts`

## 3.1 — index.css: overlay/modal классы

```css
/* Progress overlay */
.progress-overlay {
  position: fixed;
  inset: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0,0,0,0.8);
  backdrop-filter: blur(4px);
}
.progress-panel {
  background: var(--input-bg);
  border-radius: 10px;
  border: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  width: 550px;
  max-width: 90vw;
  max-height: 80vh;
}
.progress-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 14px;
  border-bottom: 1px solid var(--border);
  background: var(--input-bg);
}
.progress-header-title {
  font-size: var(--fs);
  color: var(--text-dim);
  font-weight: var(--fw-bold);
}
.progress-close {
  background: none;
  border: none;
  color: var(--text-dim);
  cursor: pointer;
  font-size: var(--fs);
  padding: 4px;
}
.progress-close:hover { color: var(--text); }
.progress-body {
  flex: 1;
  overflow-y: auto;
  padding: 10px 14px;
  min-height: 80px;
  max-height: 400px;
  font-family: var(--font-mono);
  font-size: 12px;
  line-height: 1.6;
  color: var(--text-dim);
}
.progress-console {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 6px;
  padding: 8px 10px;
  white-space: pre-wrap;
  word-break: break-all;
}
.progress-console-line { display: block; padding: 1px 0; }
.progress-console-line.renamed { color: var(--green); }
.progress-console-line.moved { color: var(--yellow); }
.progress-console-line.wrote { color: var(--text-dim); }
.progress-console-line.error { color: var(--red); }
.progress-console-line.dim { color: var(--text-disabled); }
.progress-console-sep {
  display: block;
  height: 1px;
  background: var(--border);
  margin: 4px 0;
}
.progress-footer {
  padding: 8px 14px;
  border-top: 1px solid var(--border);
  text-align: center;
}
.progress-footer.ok { color: var(--green); }
.progress-footer.fail { color: var(--red); }
.progress-bar-wrap {
  padding: 10px 14px 6px;
}
.progress-bar-labels {
  display: flex;
  justify-content: space-between;
  margin-bottom: 4px;
  font-size: 11px;
  color: var(--text-dim);
}
.progress-bar-track {
  height: 4px;
  background: var(--border);
  border-radius: 2px;
  overflow: hidden;
}
.progress-bar-fill {
  height: 100%;
  background: var(--accent);
  border-radius: 2px;
  transition: width 0.2s;
}
```

## 3.2 — ProgressOverlay.tsx: переписать

- Убрать все `style={{...}}` → заменить на CSS-классы выше
- Убрать импорт `FONT, FS, FS_SM, COLORS, ICON_BUTTON, OVERLAY_BACKDROP, MODAL_PANEL, MODAL_HEADER`
- Добавить `diff?: { from: string; to: string }[]` в пропсы
- Показывать diff в консольном стиле:
  - `-  old_name.mp3` (красным)
  - `+  new_name.mp3` (зелёным)
  - Строка-разделитель между треками
  - Подсчёт: "Renamed N files"

## 3.3 — useTagActions.ts: передать diff

Строка 142-146 — заменить формирование summary:
```ts
const diff = result.renamed?.map(r => ({ from: r.from, to: r.to })) ?? [];
// Передать diff в ProgressOverlay
dispatch({ type: 'SET_PROGRESS', payload: { ..., details: summary, diff } });
```

Добавить поле `diff` в тип прогресса в `appReducer.ts`.

## 3.4 — appReducer.ts: добавить diff в прогресс

Добавить `diff?: { from: string; to: string }[]` в интерфейс прогресса.

## 3.5 — App.tsx: передать diff

В вызове `<ProgressOverlay>` добавить проп `diff`.

## Проверка
- После apply → видна консоль с diff
- Зелёные строки для новых имён, красные для старых
- Кнопка Close работает
- Прогресс-бар во время обработки работает
