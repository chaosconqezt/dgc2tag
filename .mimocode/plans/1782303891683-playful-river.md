# План: UI/UX исправления — последовательно

Правило: 1 исправление → отчёт → проверка → следующее.

---

## Шаг 1: Убрать мёртвый CSS и JS

**Файлы:**
- `client/src/index.css` — удалить классы: `.top-panel`, `.results-panel`, `.diff-row`, `.diff-label`, `.val-new`, `.val-new.no-change`, `.card`, `.card:hover`, `.card.selected`, `.card button:hover`, `.card a:hover`
- `client/src/components/TagComparison.tsx` — удалить импорт `HEADER_STYLE` (строка 4)
- `client/src/components/TrackMatcher.tsx` — удалить импорт `HEADER_STYLE` (строка 4)
- `client/src/components/ApplyPanel.tsx` — удалить проп `localTags` из интерфейса и тела компонента
- `client/src/components/LibraryTree.tsx` — удалить проп `onCollapseAll` из интерфейса и тела компонента
- `client/src/index.css` — исправить конфликт font-family: `:root` на `*` перекрывает `body`. Убрать `font-family` из `*`, оставить только в `body`

**Верификация:** `npx tsc --noEmit` + `npm run dev`, ничего не сломалось.

---

## Шаг 2: Focus indicators — CSS

**Файл:** `client/src/index.css`

Добавить:
```css
:focus-visible {
  outline: 2px solid #ef4444;
  outline-offset: 2px;
}

input:focus-visible {
  outline: 2px solid #ef4444;
  outline-offset: -1px;
}

button:focus-visible {
  outline: 2px solid #ef4444;
  outline-offset: 2px;
}

a:focus-visible {
  outline: 2px solid #ef4444;
  outline-offset: 2px;
}
```

**Верификация:** Tab по интерфейсу — видно красную рамку фокуса на каждом элементе.

---

## Шаг 3: Hover на sidebar кнопках

**Файл:** `client/src/App.tsx`

Добавить стили `onMouseEnter`/`onMouseLeave` или CSS-класс для кнопок collapse/settings/refresh (строки 45-53). Hover: `color: '#f4f4f5'`, background: `#18181b`.

**Верификация:** Навести на каждую кнопку — цвет меняется.

---

## Шаг 4: Hover на tree items

**Файл:** `client/src/index.css`

Убрать `!important` из `.tree-item:hover` (строка 85) и сделать приоритет через specificity. Или перенести hover в inline стили в `LibraryTree.tsx`.

Текущий конфликт: inline `backgroundColor: 'transparent'` на строке 38 LibraryTree.tsx перекрывает CSS hover.

**Файл:** `client/src/components/LibraryTree.tsx`
- Добавить `onMouseEnter`/`onMouseLeave` для смены `backgroundColor` на `#18181b`

**Верификация:** Навести на папку в дереве — подсветка.

---

## Шаг 5: Hover на tag rows

**Файл:** `client/src/components/TagComparison.tsx`

Добавить `onMouseEnter`/`onMouseLeave` на каждую строку (ROW_STYLE). Hover: `backgroundColor: '#18181b'`.

**Верификация:** Навести на строку тега — лёгкая подсветка.

---

## Шаг 6: Hover на track rows

**Файл:** `client/src/components/TrackMatcher.tsx`

В `MatchRow` добавить hover: `backgroundColor: '#18181b'` при наведении на строку.

**Верификация:** Навести на трек — подсветка.

---

## Шаг 7: Hover на ApplyPanel кнопки

**Файл:** `client/src/components/ApplyPanel.tsx`

Добавить hover для кнопок WRITE & RENAME и WRITE & MOVE (сейчас нет). Использовать `filter: 'brightness(1.2)'` или `transform: 'translateY(-1px)'`.

Также добавить hover на внешние ссылки (DGC, MA) — `color: '#f4f4f5'`.

**Верификация:** Навести на каждую кнопку и ссылку — визуальная обратная связь.

---

## Шаг 8: Escape для модалок

**Файлы:**
- `client/src/components/SettingsModal.tsx` — добавить `useEffect` с `keydown` listener для Escape → `onClose()`
- `client/src/components/WebfetchOverlay.tsx` — аналогично

**Верификация:** Открыть модалку → нажать Escape → закрылась.

---

## Шаг 9: Заменить хардкод цветов на COLORS

**Приоритетные файлы (худшие):**
- `client/src/components/SettingsModal.tsx` — ~20+ мест
- `client/src/components/ApplyPanel.tsx` — ~10 мест
- `client/src/App.tsx` — ~15 мест
- `client/src/components/SearchResults.tsx` — ~6 мест
- `client/src/components/LibraryTree.tsx` — ~8 мест

Заменять по одному файлу за раз, проверять после каждого.

**Порядок:** SettingsModal → ApplyPanel → App.tsx → SearchResults → LibraryTree

**Верификация после каждого файла:** `npx tsc --noEmit`, визуально ничего не изменилось.

---

## Шаг 10: Shared Modal компонент

**Новый файл:** `client/src/components/Modal.tsx`

Вынести общую структуру из `SettingsModal` и `WebfetchOverlay`:
- Overlay с `onClick={onClose}`
- Центрированный контейнер
- Header с заголовком и кнопкой закрытия
- Escape handler (из шага 8)
- `role="dialog"`, `aria-modal="true"`

**Файлы:**
- `SettingsModal.tsx` — использовать `<Modal>`
- `WebfetchOverlay.tsx` — использовать `<Modal>`

**Верификация:** Обе модалки работают как раньше + Escape + aria.

---

## Шаг 11: Consistent spacing

Привести к единой системе:
- Panel padding: `10px`
- Panel margin: `8px`
- BorderRadius: `4px` (карточки, tree), `6px` (инпуты, кнопки), `8px` (панели)
- Убрать `3px` и `5px` borderRadius, заменить на `4px` или `6px`

**Файлы:** `styles.ts` (PANEL_STYLE, CELL_STYLE, INPUT_STYLE), все компоненты.

**Верификация:** Визуально — отступы и скругления стали консистентнее.

---

## Общая верификация (после всех шагов)

1. `npx tsc --noEmit` — нет ошибок
2. `npm run dev` — приложение запускается
3. Tab по всему интерфейсу — везде видно фокус
4. Hover на всех интерактивных элементах — визуальная обратная связь
5. Escape закрывает обе модалки
6. Цвета консистентны — нет захардкоженных hex там где должен быть COLORS
